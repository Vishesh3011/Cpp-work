#include<bits/stdc++.h>

using namespace std;

bool checkForSubArr(int a[], int n){
    int b[n];
    for(int i = 0; i < n; i++)
        b[i] = a[i];
    }
}

int main(){
    int n;
    cin >> n;

    int arr[n];
    for(int i = 0; i < n; i++)
        cin >> arr[i];

    checkForSubArr(arr, n);
    return 0;
}