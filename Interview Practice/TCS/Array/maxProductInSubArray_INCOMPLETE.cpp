#include<bits/stdc++.h>

using namespace std;

void findMaxProduct(vector <int> arr, int n){
    
}

int main(){
    int n;
    cin >> n;

    vector <int> arr(n);
    
    for(int i = 0; i < n; i++)
        cin >> arr[i];

    findMaxProduct(arr, n);
}