#include<iostream>
#include<algorithm>
#include<cstring>

using namespace std;

int main(){
    // string str;

    // cin>>str;

    // cout<<str;
    
    // string str1, str2;

    
    // string str1(5, 'n');

    // cout<<str1;

    // getline(cin, str1);

    // getline(cin, str2);

    // str1.append(str2);

    // cout<< str1;

    // cout << str1 + str2;

    // cout<<str2[2];

    // str1.clear();

    // cout<<str1;

    // if(str1.compare(str2) == 0){
    //     cout<<"Strings are equal!";
    // }

    string s;
    getline(cin, s);

    // s.clear();

    // if(s.empty())
    //     cout<<"String is empty!";

    // s.erase(3, 3);

    //cout<<s;

    cout<<s.find("def");

    // s.insert(s.length(),"lol");

    // for(int i = 0; i < s.length(); i++)
    //     cout<<s[i]<<endl;

    // cout<< s.substr(6, 4);

    // int x = stoi(s);

    // cout<<x + 2<<endl;

    // int x = 123;

    // cout<<to_string(x) + "2";

    // sort(s.begin(), s.end());

    // cout<<s;

    return 0;
}